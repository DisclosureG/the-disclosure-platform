[build]
  command   = "npm run build"
  publish   = "dist"

# The platform SPA is served from the site root. The sub-app SPA fallbacks must
# come first so deep links (e.g. /evidence/foo) resolve to the section's
# index.html, and the catch-all serves the home page for everything else.

[[redirects]]
  from   = "/peer-review/*"
  to     = "/peer-review/index.html"
  status = 200

[[redirects]]
  from   = "/evidence/*"
  to     = "/evidence/index.html"
  status = 200

[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200

[[headers]]
  for = "/assets/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"

[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options        = "DENY"
    X-Content-Type-Options = "nosniff"
    Referrer-Policy        = "strict-origin-when-cross-origin"
